---
interface Props {
    title?: string;
    subtitle?: string;
    texts: string[];
    url_image: string;
    imagePosition?: 'left' | 'right';
    border?: boolean;
}

const { title, subtitle, texts, url_image, imagePosition = 'right', border = true } = Astro.props;
---

<div class={`flex flex-col md:flex-row gap-4 items-stretch h-[550px] ${border ? 'border-l-4 border-barley-corn-400 pl-4' : ''} ${imagePosition === 'left' ? 'md:flex-row-reverse' : ''}`}>
    <div class="w-full md:w-2/5 lg:w-3/5 flex flex-col">
        {title && <h2 class="text-6xl font-bold mb-2 text-barley-corn-200">{title}</h2>}
        {subtitle && <h3 class="text-4xl font-bold mb-3">{subtitle}</h3>}
        {texts.map((text) => (
            <p class="font-normal mb-3">{text}</p>
        ))}
    </div>
    <img src={url_image} alt="Formación" class="object-cover w-full h-full md:w-3/5 lg:w-2/5" />
</div>
<style>

</style>